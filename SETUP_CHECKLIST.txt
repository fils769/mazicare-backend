╔═══════════════════════════════════════════════════════════════════════════╗
║                    LOCAL DATABASE SETUP - QUICK STEPS                     ║
╚═══════════════════════════════════════════════════════════════════════════╝

📋 STEP 1: CREATE DATABASE IN PGADMIN
────────────────────────────────────────────────────────────────────────────
1. Open pgAdmin
2. Connect to your PostgreSQL server
3. Right-click on the server → "Query Tool"
4. Copy and paste this single command:

   CREATE DATABASE mazicare_local;

5. Click "Execute" (or press F5)
6. You should see: "CREATE DATABASE" in the output

✅ Done! Database created.

────────────────────────────────────────────────────────────────────────────

📝 STEP 2: UPDATE .ENV FILE
────────────────────────────────────────────────────────────────────────────
1. Open your project folder
2. Find the .env file
3. Update the DATABASE_URL line:

   DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@localhost:5432/mazicare_local?schema=public"

   Replace YOUR_PASSWORD with your PostgreSQL password
   (Common passwords: "admin", "postgres", "root")

   Example:
   DATABASE_URL="postgresql://postgres:admin@localhost:5432/mazicare_local?schema=public"

4. Save the file

✅ Done! Connection string updated.

────────────────────────────────────────────────────────────────────────────

🔧 STEP 3: RUN MIGRATIONS
────────────────────────────────────────────────────────────────────────────
1. Open PowerShell in your project folder
2. Run this command:

   npx prisma migrate dev --name init

3. Wait for it to complete
4. You should see: "Your database is now in sync with your schema"

✅ Done! All tables created.

────────────────────────────────────────────────────────────────────────────

🌱 STEP 4: SEED THE DATABASE
────────────────────────────────────────────────────────────────────────────
1. In the same PowerShell window, run:

   npx prisma db seed

2. Wait for it to complete
3. You should see success messages about creating:
   - Admin user
   - Families
   - Caregivers
   - Elders
   - Subscriptions

✅ Done! Database populated with test data.

────────────────────────────────────────────────────────────────────────────

🔍 STEP 5: VERIFY IN PGADMIN
────────────────────────────────────────────────────────────────────────────
1. In pgAdmin, refresh the database list (right-click → Refresh)
2. Expand: mazicare_local → Schemas → public → Tables
3. You should see tables like:
   - users
   - families
   - caregivers
   - elders
   - activity_logs
   - subscriptions
   
4. Check data by running this query:

   SELECT email, role FROM users;

5. You should see the admin user and others

✅ Done! Data verified.

────────────────────────────────────────────────────────────────────────────

🚀 STEP 6: RESTART YOUR SERVER
────────────────────────────────────────────────────────────────────────────
1. Stop the current server (Ctrl+C in terminal)
2. Restart with:

   npm start

3. You should see: "Application is running on: http://localhost:3000"
4. No database connection errors

✅ Done! Server connected to local database.

────────────────────────────────────────────────────────────────────────────

🧪 STEP 7: TEST THE CONNECTION
────────────────────────────────────────────────────────────────────────────
1. Open PowerShell
2. Test login:

   $body = '{"email":"admin@mazicare.com","password":"password123"}'
   $login = Invoke-RestMethod -Uri "http://localhost:3000/auth/login" -Method Post -Body $body -ContentType "application/json"
   $login.access_token

3. If you see a token, it works!

4. Test reports endpoint:

   $headers = @{ Authorization = "Bearer $($login.access_token)" }
   $reports = Invoke-RestMethod -Uri "http://localhost:3000/admin/reports" -Headers $headers
   $reports | ConvertTo-Json

5. You should see all the statistics!

✅ Done! Everything working!

════════════════════════════════════════════════════════════════════════════

📌 ADMIN LOGIN CREDENTIALS
────────────────────────────────────────────────────────────────────────────
Email:    admin@mazicare.com
Password: password123

════════════════════════════════════════════════════════════════════════════

🛠️ TROUBLESHOOTING
────────────────────────────────────────────────────────────────────────────

Problem: "database mazicare_local does not exist"
Fix: Go back to Step 1 and create the database

Problem: "password authentication failed"
Fix: Check your password in the DATABASE_URL in .env file

Problem: "could not connect to server"
Fix: Make sure PostgreSQL service is running
     - Open Windows Services
     - Find "postgresql-x64-*"
     - Make sure it's "Running"

Problem: "relation does not exist"
Fix: Run migrations again: npx prisma migrate dev

Problem: Tables are empty
Fix: Run seed again: npx prisma db seed

════════════════════════════════════════════════════════════════════════════

📚 HELPFUL COMMANDS
────────────────────────────────────────────────────────────────────────────

View database in browser:
  npx prisma studio
  (Opens at http://localhost:5555)

Reset everything:
  npx prisma migrate reset
  (Drops all tables, re-runs migrations, and seeds)

Generate Prisma client:
  npx prisma generate

Check Prisma schema:
  npx prisma validate

════════════════════════════════════════════════════════════════════════════

✨ YOU'RE ALL SET!

Your local database is ready to use. You can now:
✅ Call all API endpoints
✅ Test the admin reports
✅ Develop your frontend
✅ View/edit data with Prisma Studio

════════════════════════════════════════════════════════════════════════════
